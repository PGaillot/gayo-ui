import { SymbolRevealDirective } from './symbol-reveal.directive';

describe('SymbolRevealDirective', () => {
  it('should create an instance', () => {
    const directive = new SymbolRevealDirective();
    expect(directive).toBeTruthy();
  });
});
