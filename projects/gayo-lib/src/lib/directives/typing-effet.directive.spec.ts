import { TypingEffetDirective } from './typing-effet.directive';

describe('TypingEffetDirective', () => {
  it('should create an instance', () => {
    const directive = new TypingEffetDirective();
    expect(directive).toBeTruthy();
  });
});
